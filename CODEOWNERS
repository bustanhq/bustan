# Review ownership. GitHub requests a review from the owner of every path a pull
# request touches, and applies the last matching entry per path.
#
# There is one maintainer today, so a catch-all is the whole truth and any
# narrower rule would resolve to the same person. Per GOVERNANCE.md, the pull
# request that adds a maintainer is also the one that splits this file by area -
# the public surface (src/bustan/__init__.py, errors, testing, docs/API_REFERENCE.md),
# the injection kernel under src/bustan/core, and the build and release path
# (.github, scripts, release, pyproject.toml, uv.lock, lefthook.yml) are the
# boundaries to split along.

* @mosesgameli
