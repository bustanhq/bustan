# The example runs in CI with no writable state, so the store lives in memory.
# A reader following the tutorial sets a path here and their tasks survive a restart.
DATABASE_PATH=:memory:
API_TOKEN=tutorial-secret-token
PAGE_SIZE=20
