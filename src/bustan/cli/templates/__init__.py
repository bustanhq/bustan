"""Files the scaffolder copies into a new project.

The package exists so the templates ship inside the wheel and can be located relative
to this module. Nothing here is imported: the files below are read as text and their
placeholders substituted, and several are not valid Python until that substitution has
happened.
"""

__all__: tuple[str, ...] = ()
