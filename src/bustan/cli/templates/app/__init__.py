"""Source templates for the application a new project starts from.

Read as text and never imported; the test templates contain ``$package_name``
placeholders that only become valid Python once the scaffolder substitutes them.
"""

__all__: tuple[str, ...] = ()
