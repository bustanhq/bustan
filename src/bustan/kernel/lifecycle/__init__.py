"""Application lifecycle hooks and the machinery that runs them.

Nothing is re-exported here: ``hooks``, ``manager`` and ``runner`` are imported by
name so the hook protocols stay importable without pulling the runner in with them.
"""

__all__: tuple[str, ...] = ()
